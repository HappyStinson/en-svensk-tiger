-- Configuration
function love.conf(t)
	t.title = "Game by Mike & Happy" -- The title of the window the game is in (string)
	t.version = "0.9.2"         -- The LÖVE version this game was made for (string)
  t.window.fsaa = 16
end